#include "libft.h"

void    ft_putstr(const char *str)
{
    int count;

    count = 0;
    if (str != NULL)
    {
        while (str[count] != '\0')
        {
            ft_putchar(str[count]);
            count++;
        }
    }
}
