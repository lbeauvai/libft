#include <stdlib.h>
#include "libft.h"

char    *ft_strnew(size_t size)
{
    char *str;

    if (!(str = ft_memalloc(size + 1)))
        return (NULL);
    if (str == NULL)
        return (NULL);
    ft_memset(str, 0, size);
    return (str);
}
