#include "libft.h"

void    ft_putstr_fd(const char *s, int fd)
{
    int count;

    count = 0;
    if (s != NULL)
    {
        while (s[count] != '\0')
        {
            ft_putchar_fd(s[count], fd);
            count++;
        }
    }
}
